# RKE2 Agent Configuration
server: https://{{ hostvars[groups['rke2_servers'][0]]['ansible_default_ipv4']['address'] }}:9345
token: "{{ hostvars[groups['rke2_servers'][0]]['rke2_token'] }}"
node-ip: "{{ ansible_default_ipv4.address }}"
